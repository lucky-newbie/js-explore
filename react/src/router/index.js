import { BrowserRouter as Router,
  Switch, Route, Link, HashRouter, withRouter } from 'react-router-dom';

import App from 'container/App';

const routeConfig = () => {
  return (
    <Router>
      
    </Router>
  )
}

export default routeConfig;
